<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle || 'Andy Roasting Co.' %></title>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Cormorant+Garamond:wght@600;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body class="site-body d-flex flex-column min-vh-100">
    <header class="brand-nav">
      <div class="page-frame">
        <div
          class="nav-inner"
          style="
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0.9rem 0;
          "
        >
          <a
            class="brand-mark"
            href="/"
            style="
              font-family: 'Cormorant Garamond', serif;
              font-size: 1.4rem;
              font-weight: 700;
              color: var(--roast-brown);
              text-decoration: none;
            "
          >
            Andy Roasting Co.
          </a>
          <nav
            class="nav-links"
            style="
              display: flex;
              gap: 1rem;
              font-weight: 600;
              color: var(--muted-ink);
            "
          >
            <a href="/shop" style="text-decoration: none; color: inherit"
              >Shop</a
            >
            <a href="/about" style="text-decoration: none; color: inherit"
              >About</a
            >
            <a href="/journal" style="text-decoration: none; color: inherit"
              >Journal</a
            >
            <% if (user) { %>
            <a href="/wishlist" style="text-decoration: none; color: inherit"
              >Wishlist</a
            >
            <a href="/orders" style="text-decoration: none; color: inherit"
              >Orders</a
            >
            <% } %>
          </nav>
          <div
            class="nav-actions"
            style="display: flex; align-items: center; gap: 0.6rem"
          >
            <a
              class="btn btn-light btn-sm"
              href="/cart"
              style="display: inline-flex; align-items: center; gap: 0.4rem"
            >
              Cart <% if (cartCount > 0) { %>
              <span class="badge rounded-pill ms-1 bg-warning text-dark"
                ><%= cartCount %></span
              >
              <% } %>
            </a>
            <% if (user) { %> <% if (user.role === 'admin') { %>
            <a class="btn btn-outline-secondary btn-sm" href="/admin">Admin</a>
            <% } %>
            <a class="btn btn-primary btn-sm" href="/orders">Account</a>
            <form action="/logout" method="POST" class="d-inline">
              <button class="btn btn-outline-secondary btn-sm" type="submit">
                Logout
              </button>
            </form>
            <% } else { %>
            <a class="btn btn-primary btn-sm" href="/login">Account</a>
            <% } %>
          </div>
        </div>
      </div>
    </header>

    <main class="page-shell flex-grow-1 py-4 py-md-5">
      <% if (flash && flash.length) { %>
      <div class="mb-4">
        <% flash.forEach((msg) => { %>
        <div
          class="alert alert-<%= msg.type === 'error' ? 'danger' : msg.type === 'info' ? 'info' : 'success' %> alert-dismissible fade show"
          role="alert"
        >
          <%= msg.message %>
          <button
            type="button"
            class="btn-close"
            data-bs-dismiss="alert"
          ></button>
        </div>
        <% }); %>
      </div>
      <% } %>

      <div class="page-content"></div>
    </main>
  </body>
</html>
