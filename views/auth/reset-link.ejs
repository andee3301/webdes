<%- include('../partials/header') %>
<section class="row justify-content-center">
  <div class="col-md-8">
    <div class="card shadow-sm">
      <div class="card-body">
        <h1 class="h4 mb-3">Reset link generated</h1>
        <p class="text-muted">
          Since this is a demo project, copy the link below to reset your password. It expires in 30 minutes.
        </p>
        <pre class="bg-light p-3 rounded"><code><%= resetLink %></code></pre>
        <a class="btn btn-primary" href="<%= resetLink %>">Open reset page</a>
      </div>
    </div>
  </div>
</section>
<%- include('../partials/footer') %>
